<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
	android:title="App Menu">
	<CheckBoxPreference
		android:key="pref_enable_app_menu"
		android:title="Toggle App Menu with Home button"
		android:summary="When this feature is turned on use Alt+D to go back to the desktop"
		android:defaultValue="true" />
    <EditTextPreference
        android:inputType="number"
        android:key="pref_app_menu_height"
        android:hint="height"
        android:defaultValue="540"
		android:title="App menu height"/>
    <EditTextPreference
        android:inputType="number"
        android:key="pref_app_menu_width"
        android:hint="width"
        android:defaultValue="650"
		android:title="App menu width"/>
    <EditTextPreference
        android:inputType="number"
        android:key="pref_app_menu_x"
        android:hint="x translation"
        android:defaultValue="2"
		android:title="App menu x translation"/>
    <EditTextPreference
        android:inputType="number"
        android:key="pref_app_menu_y"
        android:hint="y translation"
        android:defaultValue="60"
		android:title="App menu y translation"/>
    <ListPreference
        android:entries="@array/launch_modes" 
        android:dialogTitle="Theme" 
        android:entryValues="@array/launch_mode_values" 
        android:key="pref_launch_mode"
        android:title="Default app launch mode"
        android:defaultValue="standard" />
    <CheckBoxPreference
        android:key="pref_remember_launch_mode"
        android:title="Remember launch mode"
        android:summary="Remember last launch mode used for every app"
		android:defaultValue="true" />
</PreferenceScreen>
